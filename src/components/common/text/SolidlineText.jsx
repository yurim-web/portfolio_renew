const SolidlineText = ({ text }) => {
  return (
    <h1
      style={{
        color: "#00adb4",
        fontSize: "8vw",
        fontFamily: "Bold",
      }}
    >
      {text}
    </h1>
  );
};

export default SolidlineText;
